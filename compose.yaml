services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=spring_database'
      - 'POSTGRES_PASSWORD=12345'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
